# Цель: стек мониторинга (Prometheus, Grafana, экспортёры).
#
# До появления этого файла мониторинг был единственным, что каталось руками:
# `git pull && docker compose up -d` в /opt/samoylove-metrics. То есть про
# него нельзя было ответить ни «какая версия на проде», ни «что откатывать» —
# ровно те вопросы, ради которых остальное хозяйство переехало на deploy-kit.
# Мониторинг, который сам живёт вне правил, плохо годится в свидетели.
#
# Артефакт — не бинарь и не статика, а описание стека: compose-файл и конфиги
# Prometheus, Grafana, blackbox и экспортёра журналов. Раскладывается так же,
# как всё остальное (releases/<версия> + атомарный симлинк current), а
# применяется перезапуском юнита-обёртки, который поднимает compose из
# current/. Подробности контракта — в deploy/systemd/samoylove-metrics.service.

APP=metrics

# ROOT не /opt/metrics: каталог стека называется по домену и существует с
# первой установки, на него завязаны и .env, и тома docker.
ROOT=/opt/samoylove-metrics

ARTIFACT_DIR=stage
UNIT=samoylove-metrics.service

# Конфиги читают процессы в контейнерах под своими uid (Prometheus — 65534),
# монтируются они только на чтение, поэтому владелец на хосте — root.
OWNER=root:root

# «Сборки» здесь нет: в артефакт уезжает ровно то, что монтируется в
# контейнеры. Перечисление поимённо, а не `cp -r . stage`, — намеренно: так в
# релиз не попадут ни .git, ни README, ни (главное) локальный .env, если он
# у кого-то окажется в рабочем дереве.
BUILD_CMD='rm -rf stage && mkdir -p stage && cp docker-compose.yml stage/ && cp -r prometheus grafana blackbox nginxlog stage/'

# Конфиг nginx этого домена живёт в deploy-kit — путь считается от корня ТОГО
# репозитория. Пара обязательна целиком: половина без половины ничего не
# применяет, и шаги «взять конфиг / проверить / выложить» просто не
# выполняются, а выкатка остаётся зелёной. До этой строки конфиг
# metrics.samoy.love не ехал вообще ни одним автоматическим путём: он был
# применён руками один раз при установке (README, шаг 4) и с тех пор жил
# отдельно от репозитория, в котором его правят.
#
# Здесь у этого больше веса, чем у остальных сайтов: доступ к панели закрывает
# auth_basic ИЗ ЭТОГО ФАЙЛА. Пока файл не катится, правка basic-auth остаётся
# текстом в git, а расхождение с сервером обнаруживается только тем, что
# однажды перестанет спрашивать пароль.
#
# Перед включением сверено с живым сервером, чтобы выкатка не подменила
# работающую защиту:
#   • 401 на / отдаёт realm «samoy.love metrics» — ровно тот, что в конфиге;
#   • auth_basic_user_file /etc/nginx/.htpasswd-metrics — тот же файл, что
#     заводит server/bootstrap.sh и описывает README;
#   • /healthz отвечает «Prometheus Server is Healthy.» с полным набором
#     заголовков сниппета samoylove-metrics-headers.conf, то есть на сервере
#     лежит эта же редакция конфига, а не более ранняя.
# Синтаксис проверен deploy-kit/ci/nginx-check.sh на nginx 1.24 — той же
# версии, что на проде; пайплайн прогоняет его ещё раз перед выкладкой.
NGINX_CONF=nginx/sites/metrics.samoy.love.conf
NGINX_DEST=/etc/nginx/sites-available/metrics.samoy.love.conf

# NGINX_RELOAD не задан, и это не забывчивость. Он добавляет `systemctl reload
# nginx` в release.sh ПЕРЕД перезапуском юнита — статике это нужно, потому что
# nginx раздаёт ей файлы прямо из каталога релиза. Здесь он не раздаёт ничего:
# наружу идёт только проксирование на 127.0.0.1, а сам конфиг перечитывает
# nginx-apply.sh — тогда и только тогда, когда в нём есть что менять.

# Проверка после переключения — здоровье самого Prometheus, а не код 200 от
# nginx: панель за basic-auth отвечает 401 и живому стеку, и мёртвому.
# /healthz открыт без пароля и проксируется в /-/healthy Prometheus, то есть
# 200 означает «сборщик метрик поднялся с новым конфигом».
HEALTH=https://metrics.samoy.love/healthz

# Nginx общий на все домены, а перезапуск стека задевает вышестоящий прокси
# ошибками 502 — соседей проверяем полным списком.
NEIGHBOURS=samoy.love,metro.samoy.love,launcher.samoy.love,snakes.samoy.love,die.samoy.love,status.samoy.love

KEEP=5

# О релизах в чат пишет БОТ статус-страницы, а не выкатка. Правило общее для
# всех целей samoy.love: голос ровно один, иначе на каждый релиз приходят два
# сообщения — «выкачено» от Actions и «поехало» от бота, — и различать их в
# ленте приходится глазами. TELEGRAM_* поэтому заведены в одном репозитории
# из шести (у бота), а здесь их нет и заводить не надо.
#
# never, а не пустой секрет: молчание должно быть заявленным. Иначе шаг
# уведомления на каждой выкатке вешает предупреждение «о релизе никто не
# узнал», и настоящая потеря уведомлений становится неотличима от нашей —
# ровно так пять репозиториев годами молчали, и никто не заметил. Строку
# читают ДО проверки токена, так что она работает и без секретов.
#
# ОСОБЫЙ СЛУЧАЙ МОНИТОРИНГА, И ЭТО НЕ ПРИДИРКА. У остальных целей «голос
# один» означает «говорит бот». Здесь он означает тишину: бот про эту цель не
# знает вовсе — в samoy-status/config/status.json пять проектов, и metrics
# среди них нет. Сообщать ему тоже нечем: WRITE_VERSION_FILE=0 ниже, версии
# наружу не отдаётся. То есть о выкатке мониторинга сейчас не узнаёт никто и
# никак, кроме как открыв Actions.
#
# NOTIFY=never этого НЕ ухудшает: без секретов шаг уведомления и так не мог
# отправить ни одного сообщения, только предупреждение. Чинится это не здесь:
# нужен проект в конфиге статус-страницы (см. комментарий у
# WRITE_VERSION_FILE — версию агенту есть откуда взять и без version.json).
NOTIFY=never

# version.json раздавать некуда: наружу этот домен отдаёт только Grafana и
# Prometheus, и класть в них свой файл незачем. Выкатка подтверждается именем
# релиза в симлинке current (--no-version-file).
#
# Оговорка к «некуда»: технически место есть — nginx этого домена уже отдаёт
# /healthz сам, мимо обеих панелей, и точно так же мог бы отдавать файл из
# current/. Но версию мониторинга проще взять с диска: агент статус-страницы
# живёт на том же сервере и умеет читать симлинк релиза (тип сборки
# "release", как у chillhub-api и самого бота). Имя current -> releases/<имя>
# и есть версия, HTTP для этого не нужен, и снаружи ничего открывать не
# приходится — домен весь под basic-auth не просто так.
WRITE_VERSION_FILE=0

# Данные ВНЕ каталога релизов и выкатка их не касается:
#   • история Prometheus и настройки Grafana — в именованных томах docker
#     (samoylove-metrics_prometheus-data, samoylove-metrics_grafana-data).
#     Имя проекта прибито в docker-compose.yml (name: samoylove-metrics),
#     поэтому смена каталога релиза на имена томов не влияет: и выкатка, и
#     откат подхватывают ту же историю;
#   • пароль администратора Grafana — /opt/samoylove-metrics/.env, рядом с
#     releases/, но не внутри. В артефакте его нет и быть не должно.

# ------------------------------------------------------------ локальный запуск
#
# Стек мониторинга локально не поднимается, и это не вопрос усилий:
#
#   * docker-compose.yml:87 берёт env_file по АБСОЛЮТНОМУ серверному пути
#     /opt/samoylove-metrics/.env — там лежит пароль Grafana, и в артефакт он
#     сознательно не входит;
#   * node-exporter монтирует /, /run/systemd, /run/dbus и требует pid: host —
#     на Windows этого нет, а на чужой Linux-машине он показывал бы её,
#     а не прод;
#   * смысл экспортёров — наблюдать за НЕконтейнеризованными сервисами того
#     самого сервера. Локальная копия наблюдала бы за пустотой.
#
# Смотреть на метрики надо там, где они собираются: https://metrics.samoy.love.
# Конфиги Prometheus и правила проверяются CI (ci/), а не запуском стека.
RUN_SKIP="нужен Linux, host-монтирования и серверный env_file — смотрите metrics.samoy.love"

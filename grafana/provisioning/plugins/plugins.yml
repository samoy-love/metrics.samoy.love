# Плагинов-приложений стек не ставит. Файл нужен, чтобы каталог существовал:
# без него Grafana на каждом старте пишет в журнал ошибку уровня error
# «Failed to read plugin provisioning files from directory», и настоящие
# ошибки провижининга теряются среди неё.
apiVersion: 1

apps: []

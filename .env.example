# Образец. На сервере копируется в /opt/samoylove-metrics/.env и заполняется
# настоящим значением; сам .env в репозиторий не попадает (см. .gitignore).
#
#   cp .env.example .env
#   sed -i "s/ЗАМЕНИТЬ/$(openssl rand -base64 24)/" .env
#   chmod 600 .env

GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=ЗАМЕНИТЬ
# Токен бота: выдаёт @BotFather. Читает contactpoints.yml через
# $__env{TELEGRAM_BOT_TOKEN} — это единственный путь секрета к Grafana.
TELEGRAM_BOT_TOKEN=ЗАМЕНИТЬ
# Общий секрет Grafana <-> renderer. Значение одно и то же в обеих строках —
# это не два разных токена, а один и тот же, под именами, которые ждут две
# разные стороны (docker-compose.yml). Дефолт "-" Grafana 13 запрещает в
# продакшене: без этой пары сервис не стартует вовсе.
GF_RENDERING_RENDERER_TOKEN=ЗАМЕНИТЬ
AUTH_TOKEN=ЗАМЕНИТЬ

# Секреты живут только на сервере.
.env
telegram-bot-token
*.htpasswd
.htpasswd
*.key
*.pem

# Локальные настройки инструментов.
.claude/settings.local.json

# Каталог сборки: его создаёт BUILD_CMD из .deploy-kit/prod.env
# (rm -rf stage && ... && cp -r ... stage/). Это копия того, что уже лежит
# рядом, — в истории от неё только шум и конфликты при каждой выкатке.
stage/
